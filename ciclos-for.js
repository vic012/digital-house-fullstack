//loop for

for(
    // Cria a variável que será testada
    let contador = 0;
    // Avalia a condição
    contador <= 4;
    // Incrementa a variável a cada ciclo
    contador ++
    ){
        console.log(contador);

};